<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="csrf-Token" content="<%= csrfToken %>">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>
        <%= title %>
    </title>
</head>
<body style=" font-family:serif; background-size: cover; background-image: url(https://img.freepik.com/premium-photo/blurred-colorful-wallpaper-background_976742-179.jpg);">
    <div class=" mt-5 ml-40 underline decoration-blue-400 text-blue-600">
        <a href="/course/<%= courseTobeEdited.id %>" >Back To course</a>
    </div>
    <div class="flex justify-center">
        <p class="text-6xl  font-semibold text-violet-700">List of all chapters in  <%= courseTobeEdited.title %></p>
    </div>
    <hr class="mx-40 mt-5 border-t-2 border-purple-700">
    <div class="flex justify-center mt-3">
        <ul class="list-decimal">
            <% for(var i=0; i<chapterOfCourse.length ; i++) { %>
        <div class="pl-7 mt-3 rounded w-80 h-7 border shadow-lg border-purple-600 bg-gray-100 border-2">   
                    <a href="/chapter/<%= chapterOfCourse[i].id %>">
                        <li class=" decoration-blue-400 text-blue-600">
                            <%= chapterOfCourse[i].name %>
                        </li>
                    </a>
                </div>
                <% } %>
            </ul>       
        </div>
        </body>
</html>